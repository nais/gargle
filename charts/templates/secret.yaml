{{ if .Values.gargle_key }}
kind: Secret
apiVersion: v1
metadata:
  name: {{ .Release.Name }}-google-sa-key
  labels:
    app: {{ .Release.Name }}
data:
  key.json: {{ .Values.gargle_key | b64enc | quote }}
{{ end }}
